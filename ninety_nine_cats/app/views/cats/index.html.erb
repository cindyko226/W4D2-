<h2>All Cats</h2>

<ul>
    <% @cats.each do |cat| %>
    <li>Name: <%= cat.name %> </li>
    <a href="<%= cat_url(cat) %>"> Show me the cat! </a>    
    <br><br>
    <% end %>
    <a href="<%= new_cat_url %>"> Create a cat! </a>
</ul>